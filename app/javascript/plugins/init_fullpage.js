import fullpage from 'fullpage.js';

const fullPageInstance = new fullpage('#myFullpage', {
    // navigation: true,
    // sectionsColor:['#ff5f45', '#0798ec', '#fc6c7c', 'grey']
});

export { fullPageInstance }
